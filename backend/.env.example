DB_HOST=localhost
DB_PORT=3306
DB_NAME=pmo
DB_USER=root
DB_PASSWORD=ha123
PORT=5000

# JWT secret used to sign and verify auth tokens.
# A development fallback is used if this is not set, but you SHOULD set your own in production.
JWT_SECRET=pmo-dev-secret-change-me-in-production
JWT_EXPIRES_IN=1d
